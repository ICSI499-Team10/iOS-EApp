import {StyleSheet} from "react-native";

export const globalStyles = StyleSheet.create({
    stackNavStyle:{
        height:100,
        justifyContent: "top",
        alignItems: "flex-start",
        flex: 1,
        flexDirection: "row"
    }
});

